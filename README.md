# Static C-ABI libs for Bob

This project produces a static library for Windows & Linux that allows diff-ing and applying diffs through a C ABI interface.

The goal of this project is to allow non-Rust languages to use create and use Bob's patches.
